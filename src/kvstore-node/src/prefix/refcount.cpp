// LLD §3.2 — Refcount: the entire implementation is inline in the header.
// This translation unit exists so the build system has something to compile,
// keeping the per-module .cpp invariant uniform.
#include "prefix/refcount.h"
